<% if @action == :reg_group %>
    <%= render 'reg_group' %>
<% else %>
    <h2 style="text-align: center">Групи</h2>
    <div class="page_content">
      <%= render 'groups/groups' if @groups %>
    </div>
<% end %>