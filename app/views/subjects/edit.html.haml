%h2 Edit Subject '#{@subject.name}'
= form_for @subject, class: 'form' do |f|
  .form-group
    = f.label :name
    = f.text_field :name, class: 'form-control'
  %div
    = f.submit 'Update subject', class: 'btn btn-default'